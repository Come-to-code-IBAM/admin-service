{
  "name": "carnet-troupeau-admin-service",
  "version": "0.1.0",
  "description": "Service admin (NestJS, architecture hexagonale) — Carnet du troupeau",
  "license": "UNLICENSED",
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "db:generate": "prisma generate --schema src/infrastructure/database/prisma/schema.prisma",
    "db:push": "prisma db push --schema src/infrastructure/database/prisma/schema.prisma",
    "build": "nest build",
    "start": "nest start",
    "start:dev": "nest start --watch",
    "start:prod": "node dist/main",
    "lint": "eslint \"src/**/*.ts\" --fix"
  },
  "dependencies": {
    "@nestjs/common": "^11.0.0",
    "@nestjs/config": "^4.0.0",
    "@nestjs/core": "^11.0.0",
    "@nestjs/platform-express": "^11.0.0",
    "@prisma/client": "^6.1.0",
    "nanoid": "^3.3.7",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.0",
    "@types/node": "^22.10.0",
    "prisma": "^6.1.0",
    "typescript": "^5.7.0"
  }
}
